#include "main.h"
/**
* _isuppera - a function that checks for uppercase character
* @c: the character in ASCII code
*Return: 1 or 0
*/
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
return (0);
}
